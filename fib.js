// 4. Реализовать функцию которая возвращает массив с числами фибоначчи,  количество которых зависит от
// переданного значения при вызове функции.
function fibi(n) {
    var sum = 0,i,str,arr=[];
    for (i-0; i<n; i++){
        if( arr.length > 1)  sum += arr[ arr.length-2];
        if( arr.length == 1) sum = 1;
        if( arr.length == 0) sum = 0;
arr.push( sum);
    str=arr.join(",");
    }
    }
